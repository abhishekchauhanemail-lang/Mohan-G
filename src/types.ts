export interface Establishment {
  id: string;
  name: string;
  role: string;
  since: string;
  description: string;
  image: string;
  location: string;
}

export interface ConsultancyService {
  title: string;
  description: string;
}
